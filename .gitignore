.DS_Store
node_modules/
# Pulled env dumps — contain live tokens (VERCEL_OIDC_TOKEN, RECONCILE_SECRET).
env.download
# Now also carries SUPABASE_DB_URL (live DB password) — never commit this.
env.local

# Scratch Node scaffold for running SQL directly against Supabase
# (Wave 1, #584) — node_modules + a runner script, not project code.
.db-runner/

# Playwright MCP scratch: console logs, page snapshots, screenshots.
.playwright-mcp/
# Screenshots dropped at the root by browser tooling. Committed docs images
# live under docs/, which this does not touch.
/*.png
